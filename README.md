# gbps.me — Internet Speed Test

A self-hosted internet speed test measuring **download**, **upload**, **ping**, and **jitter**.

## Run locally
```bash
npm install
npm start
# open http://localhost:3000